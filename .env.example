# IB Gateway Connection Settings
IB_HOST=127.0.0.1
IB_PORT=4002
IB_CLIENT_ID=1
IB_TIMEOUT=10

# Server Settings
SERVER_HOST=0.0.0.0
SERVER_PORT=8000
SERVER_RELOAD=false

# Data Settings
CACHE_DIR=./data/cache
MAX_BARS_IN_MEMORY=10000
CACHE_MAX_AGE_HOURS=24

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log
LOG_MAX_BYTES=104857600
LOG_BACKUP_COUNT=10

# Performance
DATA_FRESHNESS_THRESHOLD_SECONDS=30
PACING_MIN_INTERVAL_SECONDS=15

# TastyTrade API (Volume Delta Streaming)
# 1. Create OAuth app at: https://my.tastytrade.com/app.html#/manage/api-access/oauth-applications
# 2. Check all scopes (read, trade), add redirect URL (e.g., http://localhost:8000)
# 3. Save client_id and client_secret
# 4. Generate refresh token: OAuth Applications -> Manage -> Create Grant
# 5. Refresh tokens never expire - store securely
TASTYTRADE_ENABLED=false
TASTYTRADE_CLIENT_SECRET=
TASTYTRADE_REFRESH_TOKEN=
TASTYTRADE_IS_SANDBOX=false
